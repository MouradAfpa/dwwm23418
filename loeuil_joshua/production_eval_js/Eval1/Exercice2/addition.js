function addition(){
    const first = parseInt(prompt("entrez un premier chiffre"));
    const second = parseInt(prompt("entrez un second chiffre"));

    let res = first + second

    document.write(first + " + " + second + " = "+ res );
}